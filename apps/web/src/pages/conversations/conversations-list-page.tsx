// This page is now handled by ConversationsLayout
// The list is built into the left panel of the layout
// This component is kept for route compatibility but doesn't render anything
export default function ConversationsListPage() {
  return null
}
